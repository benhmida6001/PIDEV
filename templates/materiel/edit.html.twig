{# filepath: /c:/Users/bahae/Documents/BenH/PIDEV/templates/materiel/edit.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Modifier le Matériel{% endblock %}

{% block stylesheets %}
{{ parent() }}
<style>
    .form-section {
        background: #f8f9fa;
        padding: 20px;
        border-radius: 10px;
        margin-bottom: 20px;
    }
    
    .form-section h5 {
        color: #667eea;
        font-weight: bold;
        margin-bottom: 20px;
        padding-bottom: 10px;
        border-bottom: 2px solid #667eea;
    }
    
    .image-preview {
        max-width: 300px;
        max-height: 300px;
        border-radius: 10px;
        margin-top: 10px;
    }
    
    .current-image {
        max-width: 200px;
        max-height: 200px;
        border-radius: 10px;
        margin-bottom: 15px;
    }
    
    .custom-file-upload {
        border: 2px dashed #667eea;
        border-radius: 10px;
        padding: 30px;
        text-align: center;
        cursor: pointer;
        transition: all 0.3s ease;
        background: white;
    }
    
    .custom-file-upload:hover {
        background: #f0f4ff;
        border-color: #4facfe;
    }
    
    .custom-file-upload i {
        font-size: 3rem;
        color: #667eea;
    }
</style>
{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <!-- Header -->
            <div class="card mb-4" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); color: white;">
                <div class="card-body text-center py-4">
                    <h2 class="mb-2">✏️ Modifier le Matériel</h2>
                    <p class="mb-0">Mettez à jour les informations de votre matériel</p>
                </div>
            </div>

            {% for message in app.flashes('success') %}
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}

            {% for message in app.flashes('error') %}
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}

            <!-- Form -->
            <div class="card">
                <div class="card-body p-4">
                    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                        
                        <!-- Informations Générales -->
                        <div class="form-section">
                            <h5><i class="bi bi-info-circle"></i> Informations Générales</h5>
                            
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    {{ form_row(form.name, {
                                        'label': 'Nom du matériel',
                                        'attr': {'class': 'form-control', 'placeholder': 'Ex: Tente de camping 4 places'}
                                    }) }}
                                </div>
                                <div class="col-md-6 mb-3">
                                    {{ form_row(form.category, {
                                        'label': 'Catégorie',
                                        'attr': {'class': 'form-select'}
                                    }) }}
                                </div>
                            </div>

                            <div class="mb-3">
                                {{ form_row(form.description, {
                                    'label': 'Description',
                                    'attr': {
                                        'class': 'form-control', 
                                        'rows': 4, 
                                        'placeholder': 'Décrivez votre matériel en détail...'
                                    }
                                }) }}
                            </div>
                        </div>

                        <!-- Quantité et État -->
                        <div class="form-section">
                            <h5><i class="bi bi-box-seam"></i> Quantité et État</h5>
                            
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    {{ form_row(form.quantity, {
                                        'label': 'Quantité totale',
                                        'attr': {'class': 'form-control', 'min': 0}
                                    }) }}
                                </div>
                                <div class="col-md-4 mb-3">
                                    {{ form_row(form.availableQuantity, {
                                        'label': 'Quantité disponible',
                                        'attr': {'class': 'form-control', 'min': 0}
                                    }) }}
                                </div>
                                <div class="col-md-4 mb-3">
                                    {{ form_row(form.status, {
                                        'label': 'État du matériel',
                                        'attr': {'class': 'form-select'}
                                    }) }}
                                </div>
                            </div>

                            <div class="alert alert-info">
                                <i class="bi bi-lightbulb"></i> <strong>Astuce :</strong> 
                                La quantité disponible doit être inférieure ou égale à la quantité totale.
                            </div>
                        </div>

                        <!-- Options de Partage -->
                        <div class="form-section">
                            <h5><i class="bi bi-share"></i> Options de Partage</h5>
                            
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <div class="form-check form-switch">
                                        {{ form_widget(form.transport, {
                                            'attr': {'class': 'form-check-input', 'role': 'switch'}
                                        }) }}
                                        {{ form_label(form.transport, 'Je peux assurer le transport', {
                                            'label_attr': {'class': 'form-check-label'}
                                        }) }}
                                    </div>
                                    <small class="text-muted">
                                        <i class="bi bi-truck"></i> Cochez si vous pouvez livrer le matériel
                                    </small>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <div class="form-check form-switch">
                                        {{ form_widget(form.visible, {
                                            'attr': {'class': 'form-check-input', 'role': 'switch'}
                                        }) }}
                                        {{ form_label(form.visible, 'Visible par la communauté', {
                                            'label_attr': {'class': 'form-check-label'}
                                        }) }}
                                    </div>
                                    <small class="text-muted">
                                        <i class="bi bi-eye"></i> Décochez pour masquer temporairement
                                    </small>
                                </div>
                            </div>
                        </div>

                        <!-- Image Upload -->
                        <div class="form-section">
                            <h5><i class="bi bi-image"></i> Photo du Matériel</h5>
                            
                            {% if materiel.image %}
                                <div class="mb-3">
                                    <p class="text-muted small mb-2">Image actuelle :</p>
                                    <img src="{{ asset('uploads/materiels/' ~ materiel.image) }}" 
                                         class="current-image img-thumbnail" 
                                         alt="{{ materiel.name }}">
                                </div>
                            {% endif %}
                            
                            <div class="custom-file-upload" id="fileUploadArea">
                                <i class="bi bi-cloud-upload"></i>
                                <p class="mb-2 mt-3"><strong>Cliquez pour changer l'image</strong></p>
                                <p class="text-muted small mb-0">JPG, PNG ou GIF (max. 5 Mo)</p>
                            </div>
                            
                            <div style="display: none;">
                                {{ form_widget(form.image, {
                                    'attr': {'id': 'materiel_image'}
                                }) }}
                            </div>
                            
                            <img id="imagePreview" class="image-preview img-thumbnail" style="display: none;" alt="Aperçu de l'image">
                            
                            {% if form.image.vars.errors|length > 0 %}
                                <div class="invalid-feedback">
                                    {% for error in form.image.vars.errors %}{{ error.message }}{% endfor %}
                                </div>
                            {% endif %}
                        </div>

                        <!-- Hidden fields -->
                        {{ form_rest(form) }}
                        
                        <!-- Action Buttons -->
                        <div class="d-flex justify-content-between align-items-center mt-4 pt-3 border-top">
                            <div>
                                <a href="{{ path('app_materiel_index') }}" class="btn btn-secondary btn-lg me-2">
                                    <i class="bi bi-arrow-left"></i> Annuler
                                </a>
                                <button type="button" class="btn btn-danger btn-lg" data-bs-toggle="modal" data-bs-target="#deleteModal">
                                    <i class="bi bi-trash"></i> Supprimer
                                </button>
                            </div>
                            <button type="submit" class="btn btn-primary btn-lg px-5">
                                <i class="bi bi-check-circle"></i> Enregistrer les Modifications
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="deleteModalLabel">
                    <i class="bi bi-exclamation-triangle"></i> Confirmer la suppression
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>Êtes-vous sûr de vouloir supprimer ce matériel ?</p>
                <p class="text-danger"><strong>Cette action est irréversible !</strong></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                {{ include('materiel/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script>
document.addEventListener('DOMContentLoaded', function() {
    const fileUploadArea = document.getElementById('fileUploadArea');
    const fileInput = document.getElementById('materiel_image');
    const imagePreview = document.getElementById('imagePreview');
    
    if (fileUploadArea && fileInput) {
        fileUploadArea.addEventListener('click', function(e) {
            e.preventDefault();
            fileInput.click();
        });
    }
    
    if (fileInput) {
        fileInput.addEventListener('change', function() {
            if (this.files && this.files[0]) {
                const reader = new FileReader();
                
                reader.onload = function(e) {
                    imagePreview.src = e.target.result;
                    imagePreview.style.display = 'block';
                    
                    fileUploadArea.innerHTML = `
                        <i class="bi bi-check-circle-fill text-success"></i>
                        <p class="mb-2 mt-3"><strong>Nouvelle image sélectionnée !</strong></p>
                        <p class="text-muted small mb-0">Cliquez pour changer à nouveau</p>
                    `;
                }
                
                reader.readAsDataURL(this.files[0]);
            }
        });
    }
});
</script>
{% endblock %}
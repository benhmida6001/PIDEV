{# filepath: /c:/Users/bahae/Documents/BenH/PIDEV/templates/base.html.twig #}
<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}GREENCORE{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🌍</text></svg>">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
            <style>
                body {
                    background: linear-gradient(180deg, #1b5e20 0%, #2e7d32 50%, #43a047 100%);
                    min-height: 100vh;
                    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                }
                .auth-container {
                    background: rgba(255, 255, 255, 0.95);
                    border-radius: 15px;
                    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
                    backdrop-filter: blur(10px);
                }
                .form-control:focus {
                    border-color: #4caf50;
                    box-shadow: 0 0 0 0.2rem rgba(76, 175, 80, 0.25);
                }
                .btn-primary {
                    background-color: #4caf50;
                    border-color: #4caf50;
                }
                .btn-primary:hover {
                    background-color: #388e3c;
                    border-color: #388e3c;
                }
                .logo {
                    font-size: 2rem;
                    font-weight: bold;
                    color: #4caf50;
                    margin-bottom: 1rem;
                }
                .navbar-brand {
                    color: #4caf50 !important;
                    font-weight: bold;
                }
                .card {
                    border: none;
                    border-radius: 15px;
                    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
                }
                .table {
                    background: white;
                    border-radius: 10px;
                    overflow: hidden;
                }
                .table thead {
                    background-color: #4caf50;
                    color: white;
                }
                /* Validation error styling (make form validation messages clearly visible) */
                .invalid-feedback {
                    color: #dc3545 !important;
                }
                .text-danger {
                    color: #dc3545 !important;
                }
                .form-control.is-invalid {
                    border-color: #dc3545;
                    box-shadow: 0 0 0 0.2rem rgba(220,53,69,0.12);
                }
                /* Additional selectors for different form error output structures */
                .form-errors,
                ul.form-errors,
                .form-error-message,
                .form-errors li {
                    color: #dc3545 !important;
                    margin-top: 0.25rem;
                }
                /* Target unordered lists that are rendered immediately after form controls
                   (Symfony's form_errors often outputs a <ul><li>...</li></ul> directly after the widget).
                   This makes the bullet text red without changing all lists globally. */
                input + ul,
                textarea + ul,
                select + ul,
                .form-control + ul,
                .form-select + ul {
                    color: #dc3545 !important;
                    margin-top: 0.25rem;
                }
                input + ul > li,
                textarea + ul > li,
                select + ul > li,
                .form-control + ul > li,
                .form-select + ul > li {
                    list-style-position: inside;
                    color: #dc3545 !important;
                }
                /* Also style error lists that appear before widgets (common with `form_row`) */
                .mb-3 > ul,
                .mb-3 ul > li,
                .col-md-6 ul > li,
                .col-md-4 ul > li,
                .form-row ul > li,
                .form-group ul > li {
                    color: #dc3545 !important;
                    list-style-position: inside;
                    margin-top: 0.25rem;
                }
            </style>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </head>
    <body>
        {% block navbar %}
            {% if app.user %}
                <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
                    <div class="container">
                        <a class="navbar-brand" href="{{ path('app_home') }}">
                            🌍 GREENCORE
                        </a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                            <span class="navbar-toggler-icon"></span>
                        </button>
                        <div class="collapse navbar-collapse" id="navbarNav">
                            <ul class="navbar-nav ms-auto">
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_materiel_index') }}">
                                        <i class="bi bi-box"></i> Ressources 
                                    </a>
                                </li>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="{{ path('app_category_index') }}">
                                            <i class="bi bi-tags"></i> Categories
                                        </a>
                                    </li>
                                {% endif %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_dashboard') }}">
                                        📊 Tableau de bord
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link d-flex align-items-center" href="{{ path('app_profile') }}">
                                        {% if app.user and app.user.profilePicture %}
                                            <img src="{{ asset('uploads/profile_pictures/' ~ app.user.profilePicture) }}" alt="Photo de profil" class="rounded-circle" style="width: 30px; height: 30px; object-fit: cover; margin-right: 8px;">
                                        {% else %}
                                            <span style="font-size: 1.2rem; margin-right: 8px;">👤</span>
                                        {% endif %}
                                        Profil
                                    </a>
                                </li>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="{{ path('app_admin_users') }}">
                                            🔧 Administration
                                        </a>
                                    </li>
                                {% endif %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_logout') }}">
                                        🚪 Déconnexion
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </nav>
            {% endif %}
        {% endblock %}

        <div class="container mt-4">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>